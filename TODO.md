# TODO

- [ ] Refactor `frontend/src/components/Blog/Category.jsx`:
  - [ ] Break large JSX into small components (filter bar + card + meta row)
  - [ ] Add mock data (`MOCK_POSTS`) with realistic fields
  - [ ] Implement working category filtering (useState)
  - [ ] Fix JSX correctness (`className`, remove nested <a>)
  - [ ] Improve readability + consistency
- [ ] (No page wiring): keep `Category.jsx` as an independent component (do not change Blog page)
- [ ] Run frontend lint/build if available (optional)
